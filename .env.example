# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# Auth / app
NEXT_PUBLIC_SITE_URL=http://localhost:3000
CRON_SECRET=

# CRM (API → sanitize → BD → UI). El front NUNCA llama al CRM.
# mock = simula la API con seed (local/tests). ego = eGO Real Estate en staging/prod.
CRM_ADAPTER=mock
CRM_API_URL=
CRM_API_KEY=
# Alias opcional usado por algunas cuentas eGO:
# CRM_API_TOKEN=
CRM_TIMEOUT_MS=15000
CRM_MAX_RETRIES=2

# IA
ANTHROPIC_API_KEY=

# Email
RESEND_API_KEY=
EMAIL_FROM=MontePego Life <noreply@montepegolife.com>

# Analítica
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=montepegolife.com
